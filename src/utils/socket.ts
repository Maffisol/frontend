/*import { io, Socket } from 'socket.io-client';

// Haal de server-URL op vanuit de Vite environment variables
const SERVER_URL = import.meta.env.VITE_SERVER_URL;

if (!SERVER_URL) {
  throw new Error('VITE_SERVER_URL is not defined in environment variables.');
}

// Initialiseer de socket
const socket: Socket = io(SERVER_URL, { transports: ['websocket'] });

export default socket;*/